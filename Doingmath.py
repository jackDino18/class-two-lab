from random import random 
from math import sqrt, acos
def main():
    #tells user to enter x coordinate
    x_user:int = input("enter the x coordinate")

    #tells user to enter y coordinate
    y_user:int = input("enter y coordinate")

    
    #creates random x coordinate
    x_enemy:int = random.seed(100)
    print(x_enemy)

    #creates random y coordinate
    y_enemy:int = random.seed(100)






